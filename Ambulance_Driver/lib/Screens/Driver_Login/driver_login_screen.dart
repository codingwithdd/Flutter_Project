import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/Driver_login/components/body.dart';

class DriverLoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
